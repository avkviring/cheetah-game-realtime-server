# Список объектов комнаты
# Для объекта - указываем префаб, группу объекта и значение полей (по-необходимости)
objects:
  - template: user
    # id можно не указывать, но иногда это необходимо для ссылок
    id: 100
    group: red
    values:
      - field: user/score
        value: 100
      - field: user/info
        value:
          name: alex
  - template: weapons/izida
      group: red
      values:
        - field: owner
          value: 100
  - template: user
    id: 200
    group: blue
    values:
      - field: user/score
        value: 100
      - field: user/info
        value:
          name: peter
  - template: weapons/izida
    group: blue
    values:
      - field: owner
        value: 200
